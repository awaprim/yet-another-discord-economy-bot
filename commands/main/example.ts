const main: any = {
  name: ["example"], // <= here you can define what's command name aka what command needs to be run to execute code
  cooldown: 4, // <= how long user needs to wait before sending another message (in seconds)
  run: async (client: any, message: any, args: any) => {


    // write your own code here!

    
  }
};

export {main}
